# Local LeCroy references

Do not commit the full LeCroy sample tree here. The installation may contain vendor-owned material and its redistribution terms are not established by this repository.

## Current local reference

```yaml
installation_root: "C:\\Users\\Public\\Documents\\LeCroy\\USB Protocol Suite"
vse_script_root: "C:\\Users\\Public\\Documents\\LeCroy\\USB Protocol Suite\\Scripts\\VFScripts"
```

## Files inspected for M0/M1

The following installed files were used as syntax/API references only:

- `VSTemplate.vs_`
- `VSTools.inc`
- `VS_constants.inc`
- `Examples/zExample1.vse`
- `Examples/zExampleTra.vse`
- `Examples/zExampleXfer.vse`
- `Examples/zzExamplePwr.vse`

If a future change depends on another installed file, record its local path and purpose here without copying the file unless licensing is explicitly cleared.

## Governance source boundary

The canonical Governance source is:

```yaml
repository: https://github.com/Gavin0099/ai-governance-framework
baseline_commit: NOT_SELECTED
```

`main` is a moving branch and is not a reproducible baseline. G0 must select and record an exact commit SHA from a clean source checkout before adoption. The dirty local checkout at `E:\BackUp\Git_EE\ai-governance-framework` is not an approved baseline source.
