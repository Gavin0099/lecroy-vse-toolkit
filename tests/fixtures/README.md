# Fixtures

The fixture directory is intentionally empty of trace binaries. M1 requires a known real `.usb` input, but trace captures can be proprietary and very large.

When a fixture is approved, add a small manifest containing its redacted identity, source/handling decision, expected observation, and replay instructions. The manifest must not imply broader decoder coverage than the fixture demonstrates.
